# Open Source Review Notes

본 문서는 시간표 생성 품질을 높이기 위해 참고한 공개 GitHub 자료와 적용 결정을 정리합니다.
코드는 복사하지 않고, 공개적으로 설명된 설계 원칙만 현재 프로젝트 구조에 맞게 반영했습니다.

## Reviewed Projects

- `mizosoft/timetable-generator`: 병렬 유전 알고리즘, 탐욕적 초기 후보 생성, Simulated Annealing을 섞은 학교 시간표 생성기입니다. 하드 제약과 소프트 제약을 분리하고 초기 후보 품질을 높이는 접근이 유용했습니다.
- `mcychan/GASchedule.py`: 엘리트 보존, 교차, 돌연변이, 적합도 0~1 정규화 방식이 명확하게 정리된 강의 시간표 생성 예제입니다.
- `werner-scholtz/kalender`: Flutter 캘린더 UI에서 이벤트 타일, 뷰 구성, 외형 커스터마이징을 분리하는 구조가 잘 정리되어 있습니다.
- `oist/course-scheduler`: 생성 결과를 CSV, 경고 파일, 시각화 산출물로 나누고 수동 변경 후 충돌 검증을 다시 수행하는 운영 흐름이 인상적이었습니다.
- `Aliz-f/tv-ad-scheduling-metaheuristics`: 제약 검증, 수리 연산, 다목적 평가를 분리한 스케줄링 메타휴리스틱 구조를 참고했습니다.

## Applied Decisions

- 상위 결과 선택 시 완전 중복만 제거하던 방식에서, 과목 코드와 요일 패턴을 함께 비교하는 다양성 필터로 확장했습니다.
- 주간 시간표 블록에 과목명뿐 아니라 분반, 담당자, 시간을 함께 표시해 캘린더 타일의 정보 밀도를 높였습니다.
- 생성 로딩 UI는 감성적 문구보다 진행률, 예상 소요 시간, 현재 검토 단계가 보이는 업무형 패널로 정리했습니다.
- 화면 문구는 `추천`, `자동`, `AI` 느낌을 줄이고 `시간표`, `조건`, `검토`, `평가 기준` 중심으로 맞췄습니다.
- Flutter 캘린더 패키지를 새로 도입하지 않고, 기존 `TimetableGrid`를 유지했습니다. 현재 앱은 주간 강의표만 필요하므로 외부 패키지 의존성을 늘리는 것보다 내부 그리드의 안정성을 유지하는 편이 낫습니다.

## Next Candidates

- 결과 상세 화면에 충돌/학점/공강 검증 리포트를 별도 섹션으로 분리합니다.
- 저장한 시간표를 다시 검증하는 `검토` 액션을 추가합니다.
- 모델 성능 측정용 fixture를 늘려 학년별, 공강별, 선택 과목별 평균 생성 시간을 추적합니다.
